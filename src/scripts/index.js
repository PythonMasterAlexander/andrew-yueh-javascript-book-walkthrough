// Dynamic menu in JS
//

function pageRouter() {
  // Get URL location
  const pathName = window.location.pathname

  // Check the location and check what page the user are on
  switch (pathName) {
    case '/':
    case '/index.html':
      break
  }
}
pageRouter()
